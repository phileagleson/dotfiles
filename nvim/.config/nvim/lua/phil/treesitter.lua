require'nvim-treesitter.configs'.setup{
    inednt = {
        enable = true
    },
    ensure_installed = 'all',
    sync_install = false,
    ignore_install = {''},
    hhighlight = {
        enable = true
    },
    autopairs = {
        enable = true
    }
}
